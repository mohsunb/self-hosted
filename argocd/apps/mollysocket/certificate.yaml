apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mollysocket
  namespace: mollysocket
spec:
  secretName: mollysocket-mohsun-az-tls
  dnsNames:
    - "mollysocket.mohsun.az"
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer
