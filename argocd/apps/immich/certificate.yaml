apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo
  namespace: forgejo
spec:
  secretName: gallery-mohsun-az-tls
  dnsNames:
    - "gallery.mohsun.az"
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer
