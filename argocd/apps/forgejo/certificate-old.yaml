apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo-old
  namespace: forgejo
spec:
  secretName: git-mohsun-az-tls
  dnsNames:
    - "git.mohsun.az"
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer
