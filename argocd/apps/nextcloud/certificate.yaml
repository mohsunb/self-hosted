apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  secretName: cloud-mohsun-az-tls
  dnsNames:
    - "cloud.mohsun.az"
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer
