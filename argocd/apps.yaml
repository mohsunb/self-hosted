apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://forge.mohsun.az/mohsunb/self-hosted.git
        revision: development
        directories:
          - path: argocd/apps/*
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://forge.mohsun.az/mohsunb/self-hosted.git
        targetRevision: development
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          enabled: true
